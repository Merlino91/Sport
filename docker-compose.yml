version: '3.8'

services:
  easysports:
    build: .
    container_name: easysports-addon
    restart: unless-stopped
    ports:
      - "7001:7001"
    environment:
      - EASYSPORTS_HOST=0.0.0.0
      - EASYSPORTS_PORT=7001
      - STREAMED_API_HOST=streamed.pk
      - STREAMED_CACHE_TTL=60
